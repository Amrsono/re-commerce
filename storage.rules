rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write access to authenticated users
      // In production, you might want to restrict this to specific folders per user
      allow read, write: if request.auth != null;
    }
  }
}
