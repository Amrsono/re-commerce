{
  "name": "recommerce-marketplace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@prisma/client": "^7.4.2",
    "prisma": "^7.4.2"
  },
  "scripts": {
    "postinstall": "npm run generate --workspace=database",
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --parallel"
  }
}
